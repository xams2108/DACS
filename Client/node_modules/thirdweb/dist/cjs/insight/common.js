"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.assertInsightEnabled = assertInsightEnabled;
const utils_js_1 = require("../chains/utils.js");
async function assertInsightEnabled(chains) {
    const chainData = await Promise.all(chains.map((chain) => (0, utils_js_1.getChainServices)(chain).then((services) => ({
        chain,
        enabled: services.some((c) => c.service === "insight" && c.enabled),
    }))));
    const insightEnabled = chainData.every((c) => c.enabled);
    if (!insightEnabled) {
        throw new Error(`Insight is not available for chains ${chainData
            .filter((c) => !c.enabled)
            .map((c) => c.chain.id)
            .join(", ")}`);
    }
}
//# sourceMappingURL=common.js.map