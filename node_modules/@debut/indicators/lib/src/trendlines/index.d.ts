import { LinesModel } from './lines.model';
import { LineDirective, Env } from './types';
import { TrendStateModel } from './trend.model';
export type { LinesModel, TrendStateModel, Env };
export declare class Indicator {
    hLineDirectives: LineDirective[];
    lLineDirectives: LineDirective[];
    trend: TrendStateModel;
    private lines;
    private step;
    private i;
    env: Env;
    localCounter: number;
    consoleWindow: boolean;
    minLog: any;
    maxLog: any;
    prevPoint: {
        x: number;
        l: number;
        h: number;
    };
    /**
     *
     * @param pars type of Env
     */
    constructor(pars: any);
    log(title: any, ...data: any[]): void;
    /**
     * Operate next candle method
     * @param o
     * @param c
     * @param h
     * @param l
     * @returns - arrow of 6 lines points
     */
    nextValue(o: number, c: number, h: number, l: number): LinesModel;
}
