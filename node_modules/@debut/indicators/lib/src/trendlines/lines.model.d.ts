import { LineDirective } from './types';
import { LineModel } from './line.model';
/**
 * Lines Model class.
 * this.index - index in lineDirectives array
 */
export declare class LinesModel {
    list: [number[], number[]];
    id: {
        [id: string]: LineModel;
    };
    lineIndex: number;
    private step;
    forkDiffH: number;
    forkDiffL: number;
    constructor(step: any);
    add(h: number, l: number, i: number, prevPoint?: any, lineID?: any): any;
    update(lineID: any, h: any, l: any, t: any): LineDirective;
    delete(lineID: any): void;
}
