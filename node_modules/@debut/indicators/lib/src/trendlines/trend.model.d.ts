import { Env } from './types';
import { LineModel } from './line.model';
import { LinesModel } from './lines.model';
/**
 * Trend state Model
 * The trendModel object use Lines and lineDirectives to estimate current trend state
 */
export declare class TrendStateModel {
    env: Env;
    in: {
        state: null | 'unknown' | 'flat' | 'rise' | 'fall' | 'squeeze';
        lineIndex: number | null;
        line: LineModel | null;
        size?: number;
    };
    is: {
        state: null | 'flat' | 'rise' | 'fall';
        lineIndex: number | null;
        line: LineModel | null;
        size?: number;
        start: {
            x: number;
            y: number;
        };
    };
    was: {
        state: null | 'flat' | 'rise' | 'fall';
        lineIndex: number;
        line: LineModel | null;
        size?: number;
        success?: boolean;
    };
    width: number;
    speed: number;
    at: number;
    duration: number;
    kdiff: number[];
    lines: LinesModel;
    constructor(lines: LinesModel, env: Env);
    hlMaxDuration: LineModel | null;
    llMaxDuration: LineModel | null;
    update(hLinesIDs: number[], lLinesIDs: number[]): void;
}
