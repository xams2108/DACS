/**
 * Money Flow Index (MFI) is a movement indicator used in technical analysis that looks at time and price
 * to measure the trading pressure — buying or selling. It is also called volume-weighted
 * Relative Strength Index (RSI), as it includes volume, unlike RSI, which only incorporates price.
 */
export declare class MFI {
    private positiveMoneyFlowSum;
    private negativeMoneyFlowSum;
    private pevTypicalPrice;
    private posCircular;
    private negCircular;
    constructor(period?: number);
    nextValue(high: number, low: number, close: number, volume: number): number;
    momentValue(high: number, low: number, close: number, volume: number): number;
}
