export declare class ATR {
    private prevClose;
    private avg;
    /**
     * Конструктор
     * @param period - период по умолчанию 14
     */
    constructor(period?: number, smoothing?: 'SMA' | 'EMA' | 'SMMA' | 'WEMA' | 'LWMA' | 'EWMA' | 'RMA');
    nextValue(high: number, low: number, close: number): any;
    momentValue(high: number, low: number): number;
}
