/**
 * Provider for fast detection highs and lows in period
 */
export declare class MaxProvider {
    private highest;
    private max;
    constructor(period: number);
    filled(): boolean;
    nextValue(high: number): number;
    momentValue(high: number): number;
}
