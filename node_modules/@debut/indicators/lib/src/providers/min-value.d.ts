/**
 * Provider for fast detection highs and lows in period
 */
export declare class MinProvider {
    private lowest;
    private min;
    constructor(period: number);
    filled(): boolean;
    nextValue(low: number): number;
    momentValue(low: number): number;
}
