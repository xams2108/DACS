import { CircularBuffer } from './circular-buffer';
export declare class Extremums extends CircularBuffer {
    period: number;
    private mode;
    private comparator;
    private prevIx;
    constructor(period: number, mode: 'max' | 'min');
    nextValue(value: number): number;
    momentValue(value: number): number;
    private maxComporator;
    private mminComporator;
    getExtremum(shallow?: boolean): number;
}
