/**
 * SuperTrend indicator is one of the hybrid custom tools that show the current trend in the market.
 * The indicator name stands for Multi Time Frame SuperTrend.
 * The tool can show the direction of the trend on several timeframes at once.
 */
export declare class SuperTrend {
    private multiplier;
    private atr;
    private prevSuper;
    private prevUpper;
    private prevLower;
    private prevClose;
    constructor(period?: number, multiplier?: number, smoothing?: 'SMA' | 'EMA' | 'SMMA' | 'WEMA' | 'LWMA' | 'EWMA' | 'RMA');
    nextValue(h: number, l: number, c: number): {
        upper: number;
        lower: number;
        superTrend: number;
        direction: number;
    };
    momentValue(h: number, l: number, c: number): {
        upper: number;
        lower: number;
        superTrend: number;
        direction: number;
    };
}
